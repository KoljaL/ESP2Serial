# ESP32 and ESP8266 Command Reference

Generated by Copilot

This file contains the terminal commands you will likely use most for:
- flashing ESP32 bridge firmware
- flashing ESP8266 target firmware (serial and OTA)
- serial monitor debugging

Assumptions from current project config:
- ESP32 project root: /Users/lasarkolja/Code/ESP2Serial
- ESP8266 project root: /Users/lasarkolja/Code/ESP2Serial/esp8266-target
- USB serial device: /dev/cu.SLAB_USBtoUART
- ESP32 monitor baud: 115200
- ESP8266 runtime monitor baud: 9600

## 1) ESP32 bridge (project root)

### Build
```bash
cd /Users/lasarkolja/Code/ESP2Serial
platformio run
```

### Flash ESP32
```bash
cd /Users/lasarkolja/Code/ESP2Serial
platformio run -t upload
```

### Monitor ESP32 bridge UART
```bash
cd /Users/lasarkolja/Code/ESP2Serial
platformio device monitor -b 115200 --port /dev/cu.SLAB_USBtoUART
```

## 2) ESP8266 target (serial first flash)

Before first serial flash:
- put ESP8266 into bootloader mode (GPIO0 low at boot)
- keep RX/TX crossed via ESP32 bridge

### Build
```bash
cd /Users/lasarkolja/Code/ESP2Serial/esp8266-target
platformio run -e ifan04_serial
```

### Flash over serial bridge
```bash
cd /Users/lasarkolja/Code/ESP2Serial/esp8266-target
platformio run -e ifan04_serial -t upload --upload-port /dev/cu.SLAB_USBtoUART
```

### Monitor ESP8266 runtime output
```bash
cd /Users/lasarkolja/Code/ESP2Serial/esp8266-target
platformio device monitor -b 9600 --port /dev/cu.SLAB_USBtoUART
```

## 3) ESP8266 OTA updates

Make sure the device is running, connected to WiFi, and OTA is available.

### Build OTA environment
```bash
cd /Users/lasarkolja/Code/ESP2Serial/esp8266-target
platformio run -e ifan04_ota
```

### Upload OTA to explicit IP (required)
```bash
cd /Users/lasarkolja/Code/ESP2Serial/esp8266-target
ESP8266_OTA_TARGET=192.168.1.123 platformio run -e ifan04_ota -t upload
```

### Upload OTA to explicit IP
```bash
cd /Users/lasarkolja/Code/ESP2Serial/esp8266-target
ESP8266_OTA_TARGET=192.168.1.123 platformio run -e ifan04_ota -t upload
```

## 4) Port lock and serial troubleshooting

### Check who is using the port
```bash
lsof /dev/cu.SLAB_USBtoUART
```

### Kill a blocking process by PID
```bash
kill <PID>
```

### Force kill if needed
```bash
kill -9 <PID>
```

## 4b) OTA IP troubleshooting

Use the device IPv4 address directly for OTA uploads.

### Upload by IP
```bash
cd /Users/lasarkolja/Code/ESP2Serial/esp8266-target
ESP8266_OTA_TARGET=192.168.1.123 platformio run -e ifan04_ota -t upload
```

Use the real IP from serial monitor output or router DHCP list.

## 5) Quick health checks

### List serial devices
```bash
ls /dev/cu.*
```

### Verify both projects still compile
```bash
cd /Users/lasarkolja/Code/ESP2Serial && platformio run
cd /Users/lasarkolja/Code/ESP2Serial/esp8266-target && platformio run -e ifan04_serial && platformio run -e ifan04_ota
```

## 6) Typical workflow

1. Flash ESP32 bridge:
```bash
cd /Users/lasarkolja/Code/ESP2Serial && platformio run -t upload
```

2. Put ESP8266 in bootloader mode and serial-flash it:
```bash
cd /Users/lasarkolja/Code/ESP2Serial/esp8266-target && platformio run -e ifan04_serial -t upload --upload-port /dev/cu.SLAB_USBtoUART
```

3. Reboot ESP8266 normal mode and open monitor:
```bash
cd /Users/lasarkolja/Code/ESP2Serial/esp8266-target && platformio device monitor -b 9600 --port /dev/cu.SLAB_USBtoUART
```

4. After WiFi works, switch to OTA:
```bash
cd /Users/lasarkolja/Code/ESP2Serial/esp8266-target && ESP8266_OTA_TARGET=192.168.1.123 platformio run -e ifan04_ota -t upload
```
